#include <bits/stdc++.h>
using namespace std;


int main(){
    stack<int> st;         // Last-in-first-out
    st.push(5); st.top(); st.pop();

    queue<int> q;          // First-in-first-out
    q.push(7); q.front(); q.pop();

        
}
